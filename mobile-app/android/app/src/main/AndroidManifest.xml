<?xml version="1.0" encoding="utf-8"?>
<!--
  LUU Y: Day chi la file AndroidManifest.xml duoc viet tay truoc, dung cho
  tham khao / ghep vao du an sau khi chay `flutter create .` (lenh nay se
  sinh toan bo thu muc android/ con thieu: gradle wrapper, build.gradle,
  res/, v.v.). Xem README.md muc "Thiet lap Android" de biet cac buoc can
  lam thu cong sau khi flutter create.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bat buoc de goi API backend qua HTTP/HTTPS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bat buoc cho Geolocator lay GPS hien tai -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Bat buoc cho image_picker chup anh/quay video -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Doc/ghi file tam khi luu anh/video vao thu muc app (Android <= 12) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:label="XPHC"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!--
          Khong can API key nao ca: ban do dung flutter_map + OpenStreetMap/
          Esri World Imagery (mien phi), khong phai Google Maps SDK.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Bat buoc tu Android 11 (API 30) de image_picker mo dung ung dung camera -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.media.action.VIDEO_CAPTURE" />
        </intent>
    </queries>

</manifest>
